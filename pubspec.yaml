name: geo_alert_app
description: A geographic alert mobile client.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  firebase_core: ^3.0.0
  firebase_messaging: ^15.0.0
  geolocator: ^11.0.0

flutter:
  uses-material-design: true
